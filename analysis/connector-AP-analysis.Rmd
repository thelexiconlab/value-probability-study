---
title: "connector-AP"
output: html_document
date: "2023-02-27"
---

# import packages

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(tidyverse)
library(ggplot2)
library (ggthemes)
library(rstatix)
library(ggpubr)
library(emmeans)
library(dplyr)

options(digits=15)

```

## import data

```{r}
pilot = read_csv("compiled_pilot.csv") %>% filter(!is.na(accuracy)) %>%
  filter(is.na(type_of_trial))

visits = read_csv("../data/guess_visit_counts.csv")
guess_scores = read_csv("../data/guess_scores.csv") %>% rename(words = guess)

# merging with data
pilot = pilot %>% left_join(guess_scores)  %>%
  separate(wordpair, into = c("w1", "w2"), sep = "-") %>%
  left_join(visits %>% rename(w1=word, w1_visit_count = visit_count, w1_budget = budget)) %>%
  left_join(visits %>% rename(w2=word, w2_visit_count = visit_count, w2_budget = budget))
```


